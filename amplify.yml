version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefix web
    build:
      commands:
        - npm run build --prefix web
  artifacts:
    baseDirectory: web/dist
    files:
      - '**/*'
  cache:
    paths:
      - web/node_modules/**/*
